#! dockerfile
FROM node:slim

COPY ./backend /opt/app
RUN apt-get update \
    && apt-get install -y build-essential libstdc++6 python \
    && cd /opt/app \
    && yarn install


WORKDIR /opt/app

CMD ["yarn", "run", "dev"]
